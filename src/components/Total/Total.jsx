// total that is increased as pizzas are added to orders. Carries over in each url
import { useSelector, useDispatch } from 'react-redux';

const Total = () => {
    const orderTotal = useSelector(store => store.orderTotal);

    // let priceTotal = 
    return (<>hi</>);
}

export default Total;